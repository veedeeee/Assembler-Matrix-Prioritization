# Project Configuration — Assembler Matrix Prioritization

Agent skills that need to know the target repository should read this file.

```yaml
repo: veedeeee/Assembler-Matrix-Prioritization
workflow_create_pr: create-pr-to-master.yml
```
